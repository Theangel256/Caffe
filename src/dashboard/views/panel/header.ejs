<nav class="navbar navbar-expand-lg color-gris-web shadow-lg">
  <a class="navbar-brand" href="/dashboard/<%= guild.id %>">
    <img class="responsive-img circle" src="<%= guild ? guild.iconURL({ dynamic: true }) : null %>" 
    width="60" height="60" style="border: 1px; border-radius: 150px;"><%= guild.name%>
  </a>
  <div class="collapse navbar-collapse">
    <ul class="navbar-nav">
      <li class="nav-item"><button class="btn wrapper" id="opciones">Opciones</button></li>
      <li class="nav-item"><button class="btn wrapper" id="datos">Estadisticas</button></li>
      <li class="nav-item"><a class="btn wrapper" href="/leaderboard/<%= guild.id %>" role="button">Leaderboard</a></li>
    </ul>
  </div>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mover-right">
      <% if(login === true) { %>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="" id="navbarDropdown" role="button" data-toggle="dropdown"
          aria-haspopup="true" aria-expanded="false">
          <img class="circle responsive-img"
            src="<%= bot.users.resolve(user.id).displayAvatarURL({ dynamic: true}) %>" width="30"
            height="30" style="border: 10px; border-radius: 150px;" alt="<%=user.name%>">
          <%= textLogin %></a>
        <div class="dropdown-menu animate slideIn" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href="/logout">Salir</a>
        </div>
      </li>
      <% } else { %>
      <li class="nav-item">
        <a class="nav-links" href="/signin">Login</a>
      </li>
      <% } %>
    </ul>
  </div>
</nav>