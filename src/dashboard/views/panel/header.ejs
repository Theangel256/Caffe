<nav class="navbar navbar-expand-lg color-gris-web shadow-lg">
  <a class="navbar-brand">
    <b><%= guild.name%></b></a>
  <div class="collapse navbar-collapse">
    <ul class="navbar-nav">
      <li class="nav-item"><button class="btn font-weight-bolder wrapper" id="opciones">Opciones</button></li>
      <li class="nav-item"><button class="btn font-weight-bolder wrapper" id="datos">Estadisticas</button></li>
      <li class="nav-item"><a class="btn btn-lg" href="/leaderboard/<%= guild.id %>" role="button">Leaderboard</button></li>
    </ul>
  </div>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav font-weight-bolder mover-right">
      <% if(login === true) { %>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="" id="navbarDropdown" role="button" data-toggle="dropdown"
          aria-haspopup="true" aria-expanded="false">
          <img class="circle responsive-img"
            src="<%= bot.users.resolve(user.id).displayAvatarURL({format:'jpg', dynamic: true}) %>" width="30"
            height="30" style="border: 10px; border-radius: 150px;" alt="<%=user.name%>">
          <%= textLogin %></a>
        <div class="dropdown-menu animate slideIn" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href="/logout">Salir</a>
        </div>
      </li>
      <% } else { %>
      <li class="nav-item">
        <a class="nav-links" href="/signin">Login</a>
      </li>
      <% } %>
    </ul>
  </div>
</nav>